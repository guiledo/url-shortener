name: Keep Alive Render and Supabase

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Server
        run: |
          curl -s https://url-shortener-knyo.onrender.com/api/health
          curl -s -o /dev/null https://bgahwcxrznnkfzgqqslg.supabase.co
